# FIFO Layer UI

Route: `/inventory/costing/layers` (alias `/fifo-layers`)

Lists `InventoryCostLayer` with item/warehouse names. Detail shows consumption history → issue cost entries. Consumption evidence also on cost entry detail for issues.
