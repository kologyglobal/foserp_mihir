# Specific Identification UI

Route: `/inventory/costing/specific`

Open layers with serial/lot/pool identity via `GET …/specific`. Unidentified open layers highlighted (`SPECIFIC_COST_NOT_IDENTIFIED`). Filter: unidentified only.
