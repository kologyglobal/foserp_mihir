# Phase 5B Immutability Rules

- Posted `ProductionWipMovement` rows are not edited or deleted.
- Physical stock changes only via Inventory compensating movements.
- Stage ledger history is never rewritten by transfers.
- Cancel/reverse of WIP is Phase 5C (compensating movement + correction link).
