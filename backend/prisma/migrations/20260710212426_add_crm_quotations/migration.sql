-- Historical migration name recorded in this environment's `_prisma_migrations`.
-- Schema equivalent is covered by `20260710221000_add_crm_quotations` (already applied).
-- No-op placeholder so `migrate deploy` can proceed past migration-history drift.
SELECT 1;
