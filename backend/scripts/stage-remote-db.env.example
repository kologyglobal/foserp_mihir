# Copy to stage-remote-db.local.ps1 (gitignored) and set your password there:
#   $env:DB_PASS = '...'
#   . .\scripts\stage-remote-db.local.ps1
#
# Or set once in PowerShell before running debug scripts:
#   $env:DB_PASS = 'your-password'
#
# Stage MySQL (Hostinger remote — no SSH required):
#   DB_HOST=srv1491.hstgr.io
#   DB_PORT=3306
#   DB_NAME=u233611619_foserp
#   DB_USER=u233611619_erpuser_jul
#
# Debug PR approve 500:
#   cd backend
#   $env:DB_PASS = '...'
#   .\scripts\stage-pr-approve-debug.ps1
#
# Steps only:
#   .\scripts\stage-pr-approve-debug.ps1 -Action Migrate
#   .\scripts\stage-pr-approve-debug.ps1 -Action Audit
#   .\scripts\stage-pr-approve-debug.ps1 -Action Probe -PrId '4df7a5be-...'
#
# SQL fix (phpMyAdmin): scripts/live-fix-pr-approve-500.sql
